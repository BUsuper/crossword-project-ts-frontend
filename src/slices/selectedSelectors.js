export const selectIsVerticalSelection = (state) =>
  state.selected.isVerticalSelection;
export const selectSelectedCell = (state) => state.selected.selectedCell;
export const selectSelectedCrossword = (state) =>
  state.selected.selectedCrossword;
